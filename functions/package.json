{
  "name": "whats-cookin-functions",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "@google/genai": "^1",
    "firebase-admin": "^13",
    "firebase-functions": "^6"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5"
  }
}
